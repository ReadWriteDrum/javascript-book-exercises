// What value does the following expression evaluate to?

parseInt('3.1415')

// The expression evaluates to 3 because when parseInt will convert a string to a number if the string starts with a digit, but as soon as it comes across something other than a digit, it will stop and ignore the rest. 