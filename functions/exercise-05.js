// What does the following code log to the console?

function scream(words) {
  return words + '!!!!';
}

scream('Yipeee');

/*
  The code doesn't log anything to the console because the
  'console.log() statement isn't in the code.
*/  
